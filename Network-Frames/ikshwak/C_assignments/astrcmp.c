#include <stdio.h>
#include <string.h>

int astrcmp(const char *s1, const char *s2) {

    if (strcmp(a, b) == 0)
        printf("Strings are equal\n");
    else
        printf("Strings are NOT equal\n");

    return 0;
}
`